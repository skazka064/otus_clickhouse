
### Установите Apache Kafka удобным способом

```
# ============================================
  # APACHE KAFKA
  # ============================================

  zookeeper-kafka:
    image: confluentinc/cp-zookeeper:latest
    container_name: zookeeper-kafka
    hostname: zookeeper-kafka
    environment:
      ZOOKEEPER_CLIENT_PORT: 2182
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2182:2182"
    restart: unless-stopped

  kafka:
    image: confluentinc/cp-kafka:7.4.0
    container_name: kafka
    hostname: kafka
    depends_on:
      - zookeeper-kafka
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper-kafka:2182
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_LISTENERS: PLAINTEXT://:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT
    ports:
      - "9092:9092"
    restart: unless-stopped
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: kafka-ui
    hostname: kafka-ui
    depends_on:
      - kafka
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
      KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper-kafka:2182
    ports:
      - "8089:8080"
    restart: unless-stopped
```

### Установите ClickHouse и выполните необходимые настройки для работы с Kafka.
### Запишите данные в Kafka и настройте пайплайн чтения через Kafka Engine, затем переместите их в таблицу MergeTree с помощью Materialized View.

```
clickhouse-01:
    image: clickhouse/clickhouse-server:latest
    hostname: clickhouse-01
    container_name: clickhouse-01
    volumes:
      - clickhouse_data_01:/var/lib/clickhouse
      - ./config/config_01.xml:/etc/clickhouse-server/config.d/config.xml:ro
      - ./config/users/default-user.xml:/etc/clickhouse-server/users.d/default-user.xml:ro
      #- ./config/storage_config.xml:/etc/clickhouse-server/config.d/storage_config.xml:ro
      #- ./config/backup_config.xml:/etc/clickhouse-server/config.d/backup_config.xml:ro
    ports:
      - "8123:8123"
      - "9000:9000"
    depends_on:
      - zookeeper
    # - minio
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    restart: unless-stopped

настраиваем clickhouse

clickhouse-01 :) CREATE TABLE test_db.kafka_test
(
    `message` String,
    `_timestamp` DateTime DEFAULT now()
)
ENGINE = MergeTree()
ORDER BY _timestamp;

CREATE TABLE test_db.kafka_test
(
    `message` String,
    `_timestamp` DateTime DEFAULT now()
)
ENGINE = MergeTree
ORDER BY _timestamp


clickhouse-01 :) CREATE TABLE test_db.kafka_queue
(
    `message` String
)
ENGINE = Kafka
SETTINGS
    kafka_broker_list = 'kafka:9092',
    kafka_topic_list = 'clickhouse_topic',
    kafka_group_name = 'clickhouse_group_final',
    kafka_format = 'CSV',
    kafka_num_consumers = 1;

clickhouse-01 :) CREATE MATERIALIZED VIEW test_db.kafka_test_queue_mv TO test_db.kafka_test
AS SELECT message
FROM test_db.kafka_queue;

```

### Проверьте корректность чтения данных из Kafka в ClickHouse.

```
docker exec -it kafka bash
[appuser@kafka ~]$ echo "fresh_message04" | kafka-console-producer --bootstrap-server localhost:9092 --topic clickhouse_topic
[appuser@kafka ~]$ echo "fresh_message0000777" | kafka-console-producer --bootstrap-server localhost:9092 --topic clickhouse_topic
[appuser@kafka ~]$ echo "otus__777" | kafka-console-producer --bootstrap-server localhost:9092 --topic clickhouse_topic
[appuser@kafka ~]$
clickhouse-01 :) SELECT * FROM test_db.kafka_test ORDER BY _timestamp DESC;

SELECT *
FROM test_db.kafka_test
ORDER BY _timestamp DESC

Query id: 9df96220-4f94-4397-8579-6759cfe47b53

   ┌─message─────────────────┬──────────_timestamp─┐
1. │ otus__777               │ 2026-07-15 18:20:43 │
2. │ fresh_message0000777    │ 2026-07-15 18:20:26 │
3. │ fresh_message04         │ 2026-07-15 18:12:10 │
4. │ fresh_message02         │ 2026-07-15 18:10:09 │
5. │ fresh_message           │ 2026-07-15 18:06:44 │
6. │ test_message_1784021792 │ 2026-07-15 18:04:31 │
7. │ test_after_fix          │ 2026-07-15 18:04:31 │
8. │ test_message_1784021792 │ 2026-07-15 17:57:45 │
9. │ test_after_fix          │ 2026-07-15 17:57:45 │
   └─────────────────────────┴─────────────────────┘

9 rows in set. Elapsed: 0.004 sec.

```
# Итоговый результат:

## Kafka → ClickHouse работает
## Данные читаются автоматически
## Новые сообщения появляются в таблице
## Всё стабильно

