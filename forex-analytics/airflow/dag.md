## Инверсия валютных курсов

Alpha Vantage отдаёт курсы в формате **USD → Валюта**, но для торговли нам нужны привычные котировки:

| Валюта | Что отдаёт API | Что нам нужно | Преобразование |
|--------|----------------|---------------|----------------|
| **EUR/USD** | 1 USD = 0.85 EUR | 1 EUR = 1.17 USD | `rate = 1 / rate` |
| **GBP/USD** | 1 USD = 0.77 GBP | 1 GBP = 1.30 USD | `rate = 1 / rate` |
| **USD/JPY** | 1 USD = 149.87 JPY | 1 USD = 149.87 JPY | `rate = rate` (без изменений) |

```python
# Инвертируем курс для EUR/USD и GBP/USD
if invert:
    rate = 1 / rate  # 0.85 → 1.17


```

    # Почему инверсия?
# Alpha Vantage отдаёт: 1 USD = 0.85 EUR
# Нам нужно: 1 EUR = 1.17 USD
rate = 1 / rate  # Инвертируем

## ⚠️ Ограничения Alpha Vantage API

| Параметр | Значение |
|----------|----------|
| **Запросов в минуту** | 5 (free tier) |
| **Запросов в день** | 500 |
| **Запросов за запуск DAG** | 3 |
| **Пауза между запросами** | 2 секунды (`sleep(2)`) |

---

### Расчёт нагрузки

| Метрика | Значение |
|---------|----------|
| Запусков DAG в день | 24 (каждый час) |
| Запросов за запуск | 3 |
| **Всего запросов в день** | **72** |
| Доступно по лимиту | 500 |
| **Запас** | **428 запросов/день (85% свободно)** |

```python
# Пауза между запросами
curl -s "https://...eur" > /tmp/eur_usd.json
sleep 2  # Ждём 2 секунды перед следующим запросом

curl -s "https://...gbp" > /tmp/gbp_usd.json
sleep 2  # Ждём 2 секунды перед следующим запросом

curl -s "https://...jpy" > /tmp/usd_jpy.json
```

-- Таблица в ClickHouse
CREATE TABLE forex_data.ticks (
    symbol   String,     -- 'EUR/USD', 'GBP/USD', 'USD/JPY'
    bid      Float64,    -- Цена покупки (спред -0.1%)
    ask      Float64,    -- Цена продажи (спред +0.1%)
    timestamp DateTime   -- Время загрузки
) ENGINE = MergeTree()
ORDER BY (symbol, timestamp);

-- Пример вставки
INSERT INTO forex_data.ticks (symbol, bid, ask, timestamp)
VALUES 
    ('EUR/USD', 1.1234, 1.1245, '2026-07-23 14:00:00'),
    ('GBP/USD', 1.2987, 1.2998, '2026-07-23 14:00:00'),
    ('USD/JPY', 149.876, 149.987, '2026-07-23 14:00:00');

5. Текст для устного доклада
Вступление (30 секунд):

    "Для получения трендового подтверждения мы используем DAG forex_alpha_hourly, который запускается каждый час. Он загружает актуальные курсы валют из внешнего API — Alpha Vantage — и сохраняет их в ClickHouse."

Техническая реализация (1 минута):

    "DAG состоит из одной задачи — load. Она делает три запроса к Alpha Vantage для пар EUR/USD, GBP/USD и USD/JPY. Важно отметить: Alpha Vantage отдаёт курсы в формате USD → EUR и USD → GBP, поэтому мы инвертируем эти значения, чтобы получить привычные котировки. Для USD/JPY инверсия не требуется.

    Между запросами мы добавляем паузу в 2 секунды, чтобы не превысить лимиты бесплатного API — 5 запросов в минуту."

Обработка данных (45 секунд):

    "После получения данных Python-скрипт парсит JSON-ответы, извлекает курсы и формирует SQL-запрос для вставки в ClickHouse. В таблицу ticks попадают: символ валютной пары, цена покупки (bid), цена продажи (ask) с искусственным спредом ±0.1%, и временная метка."

Интеграция с пайплайном (30 секунд):

    "Эти данные используются для трендового подтверждения в нашей бизнес-логике. В отличие от потоковых сигналов из Kafka, которые приходят мгновенно, данные из Airflow дают нам часовой тренд — более стабильную картину рынка. Если сигнал из Kafka и тренд из Airflow совпадают — мы принимаем торговое решение."

6. Вопросы, которые могут задать на защите
Вопрос 1: "Почему именно Alpha Vantage?"

    "Мы выбрали Alpha Vantage, потому что:

        Бесплатный API с достаточной точностью

        Поддерживает все нужные валютные пары

        Стабильная работа с 2017 года

        Альтернатива — платные API (Twelve Data, OANDA)"

Вопрос 2: "Что делать, если API не отвечает?"

    "В DAG предусмотрены retries — при ошибке задача перезапускается через 2 минуты, до 2 раз. Также в коде есть обработка ошибок — если ответ не содержит данных, скрипт выведет предупреждение и не попытается вставить пустые строки."

Вопрос 3: "Почему спред ±0.1%?"

    "Это симуляция рыночного спреда. В реальном брокере он может быть меньше, но для целей демонстрации и тестирования торговых сигналов 0.1% — адекватное значение, приближенное к реальности."

Вопрос 4: "Как часто обновляются данные в сравнении с Kafka?"

    "Kafka даёт данные в реальном времени — каждую секунду. Airflow — раз в час. Это разные горизонты планирования:

        Kafka → мгновенные сигналы (scalping)

        Airflow → трендовое подтверждение (swing trading)"

        7. Резюме (1 слайд)

# Ключевые выводы

- ✅ **DAG полностью автоматизирован** — запускается каждый час
- ✅ **Интегрирован с ClickHouse** — данные сразу доступны
- ✅ **Учитывает ограничения API** — паузы между запросами
- ✅ **Обрабатывает ошибки** — retries и проверка ответов
- ✅ **Используется в бизнес-логике** — трендовое подтверждение

---

📈 **РЕЗУЛЬТАТ:** 3 валютные пары × 24 часа = **72 записи в день**

---

## Детализация

| Параметр | Значение |
|----------|----------|
| Валютные пары | EUR/USD, GBP/USD, USD/JPY |
| Частота | Каждый час (24 раза/день) |
| Записей в день | 3 × 24 = 72 |
| Записей в месяц | ~2,160 |
| Записей в год | ~26,280 |

---

## Преимущества DAG

| № | Преимущество | Описание |
|---|--------------|----------|
| 1 | **Автоматизация** | Не требует ручного вмешательства |
| 2 | **Интеграция** | Данные сразу попадают в ClickHouse |
| 3 | **Адаптивность** | Учитывает лимиты бесплатного API |
| 4 | **Надёжность** | Retries при ошибках |
| 5 | **Практическая ценность** | Используется для торговых решений |


        
    
